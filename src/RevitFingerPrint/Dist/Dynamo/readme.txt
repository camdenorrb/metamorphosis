Build the Release configuration of src/Metamorphosis.sln (including the
MetamorphosisDynamo project), then copy the output DLLs into this folder.

Note: pre-built DLLs are intentionally not committed to this repository. The previously
committed binaries were stale and bundled outdated dependencies with known
vulnerabilities. Always build from source or use the official signed installer.
